# Disjoint k-mer sets

## Installation

`cargo build -r`

# Usage

```
Usage: DKS <COMMAND>

Commands:
  build   
  lookup  
  help    Print this message or the help of the given subcommand(s)

Options:
  -h, --help  Print help
```

